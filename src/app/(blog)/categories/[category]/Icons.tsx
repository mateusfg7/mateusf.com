'use client'

import { FolderOpen as FolderOpenIcon, IconProps } from '@phosphor-icons/react'
import React from 'react'

export const FolderOpen: React.FC<IconProps> = ({ ...props }) => (
  <FolderOpenIcon {...props} />
)
